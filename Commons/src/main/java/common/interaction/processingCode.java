package common.interaction;

public enum processingCode {

/**
 * Enum of all processing codes.
 */
    OK,
    ERROR,
    OBJECT,
    UPDATE_OBJECT,
    SCRIPT,
    REMOVE_OBJECT,
    SORT_SPEED,
    PUT,
    POST,
    GET,
    DELETE,
}
